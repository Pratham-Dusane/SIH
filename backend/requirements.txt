fastapi>=0.110.0
uvicorn[standard]>=0.28.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.1
requests>=2.31.0
httpx>=0.27.0
firebase-admin>=6.5.0
python-jose[cryptography]>=3.3.0
python-multipart>=0.0.9
rasterio>=1.3.9
scikit-image>=0.22.0
scipy>=1.12.0
numpy>=1.26.0
pillow>=10.2.0
pytest>=8.0.0
pytest-asyncio>=0.23.0

# --- Phase 4: Google Earth Engine (PRD 7.2) ---
earthengine-api>=1.4.0

# --- Phase 7: Evidence, Outputs & Reporting (PRD §10) ---
jinja2>=3.1.0
# WeasyPrint is the primary PDF renderer. Requires GTK/Cairo native libs on
# Windows — see https://doc.courtbouillon.org/weasyprint/stable/first_steps.html
# Falls back to xhtml2pdf automatically if WeasyPrint is unavailable.
weasyprint>=60.0
xhtml2pdf>=0.2.11           # pure-Python fallback (no native deps)
shapely>=2.0.0              # mask → polygon vectorisation
pyproj>=3.6.0               # CRS reprojection for GeoJSON export
